#include <stdio.h>
#include <stdlib.h>
#include "main.h"
#include <math.h>
/**
 * _abs- prints the absolute of the argument
 * @int: parameter
 *
 * Return: 0 if successful
 */
int _abs(int);

int _abs(int x)
{
	int abs(int x);
	if (x < 0)
	{
		x *= -1;
	}
	return (x);
}
